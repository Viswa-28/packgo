---
import site from '../content/site.json';
import Icon from './Icon.astro';
import { wa, tel, phone } from '../lib/contact';

const { brand, contact, footer } = site;

// Address, email and hours only appear once the owner has filled them in.
const hasAddress = Boolean(contact.streetAddress);

const legalLinks = footer.legalLinks as { label: string; href: string }[];
---

<footer class="bg-forest pt-14 pb-28 text-cream/80 md:pb-12">
  <div class="container-x">
    <div class="grid gap-10 md:grid-cols-2 lg:grid-cols-4">
      <div>
        <p class="font-display text-2xl tracking-wide text-cream">{brand.name}</p>
        <p class="mt-2 text-[0.9375rem] text-gold">{brand.tagline}</p>
        <address class="mt-4 text-[0.9375rem] not-italic">
          {hasAddress && <span class="block">{contact.streetAddress}</span>}
          <span class="block">
            {contact.addressLocality}, {contact.addressRegion}
          </span>
          {contact.postalCode && <span class="block">{contact.postalCode}</span>}
        </address>
      </div>

      <nav aria-label="Footer">
        <h2 class="font-display text-lg tracking-wide text-cream">{footer.quickLinksHeading}</h2>
        <ul class="mt-3 space-y-1">
          {
            footer.quickLinks.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="inline-flex min-h-11 items-center text-[0.9375rem] transition-colors hover:text-gold"
                >
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </nav>

      <div>
        <h2 class="font-display text-lg tracking-wide text-cream">{footer.contactHeading}</h2>
        <ul class="mt-3 space-y-1">
          <li>
            <a
              href={tel}
              class="inline-flex min-h-11 items-center gap-2.5 text-[0.9375rem] transition-colors hover:text-gold"
            >
              <Icon name="phone" size={17} class="shrink-0 text-gold" />
              {phone}
            </a>
          </li>
          <li>
            <a
              href={wa(`Hi ${brand.name}, I have a question.`)}
              target="_blank"
              rel="noopener"
              class="inline-flex min-h-11 items-center gap-2.5 text-[0.9375rem] transition-colors hover:text-gold"
            >
              <Icon name="whatsapp" size={17} class="shrink-0 text-gold" />
              WhatsApp
            </a>
          </li>
          {
            contact.email && (
              <li>
                <a
                  href={`mailto:${contact.email}`}
                  class="inline-flex min-h-11 items-center gap-2.5 text-[0.9375rem] transition-colors hover:text-gold"
                >
                  <Icon name="mail" size={17} class="shrink-0 text-gold" />
                  {contact.email}
                </a>
              </li>
            )
          }
          <li>
            <a
              href={contact.instagramUrl}
              target="_blank"
              rel="noopener"
              class="inline-flex min-h-11 items-center gap-2.5 text-[0.9375rem] transition-colors hover:text-gold"
            >
              <Icon name="instagram" size={17} class="shrink-0 text-gold" />
              @{contact.instagramHandle}
            </a>
          </li>
        </ul>
      </div>

      {
        contact.hoursDisplay && (
          <div>
            <h2 class="font-display text-lg tracking-wide text-cream">{footer.hoursHeading}</h2>
            <p class="mt-3 flex items-start gap-2.5 text-[0.9375rem]">
              <Icon name="clock" size={17} class="mt-1 shrink-0 text-gold" />
              {contact.hoursDisplay}
            </p>
          </div>
        )
      }
    </div>

    <div
      class="mt-10 flex flex-col gap-3 border-t border-cream/15 pt-6 text-sm sm:flex-row sm:items-center sm:justify-between"
    >
      <p>{footer.copyright}</p>
      {
        legalLinks.length > 0 && (
          <ul class="flex gap-5">
            {legalLinks.map((link) => (
              <li>
                <a href={link.href} class="transition-colors hover:text-gold">
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        )
      }
    </div>
  </div>
</footer>
